import { NS } from "@ns";

export async function main(ns: NS): Promise<void> {
    // check if we have the TOR node

    // if not buy TOR node when we have the money

    // run script to buy from TOR node (while loop)

    // run search script
    // return internet

    // run getRoot script (while loop)
    
    // copypayload

    // attack (while loop w/ search)

    // buy servers (while loop)

    // buy hacknet (while loop)

    // progress through milestones
    
}